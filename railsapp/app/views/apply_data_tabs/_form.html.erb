<%= form_with(model: apply_data_tab) do |form| %>
  <% if apply_data_tab.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(apply_data_tab.errors.count, "error") %> prohibited this apply_data_tab from being saved:</h2>

      <ul>
        <% apply_data_tab.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
